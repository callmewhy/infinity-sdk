import { BaseModule } from './base'

/**
 * Settings can be used to provide configuration information for modules, and can be retrieved in the context by using the settings name.
 */
export abstract class Settings extends BaseModule {}
