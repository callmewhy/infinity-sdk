import { ContextModule } from './base'

/**
 * Perception is a perception node for interacting with the external world, which can perform read and write operations and does not depend on context.
 */
export class Perception extends ContextModule {}
